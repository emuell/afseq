[package]
name = "afseq"
version = "0.1.0"
edition = "2018"
description = "afplay sequencer"
authors = ["Eduard Müller <mail@emuell.net>"]
exclude = ["assets", "examples"]
categories = ["audio", "player", "sequencer"]

[dependencies]
paste = { version = "1.0.9"}
rhai = { version = "1.10.1", optional = true, features = ["metadata"] }
numtraits = { version = "0.0.1" }

[dev-dependencies]
afplay = { path = "../afplay", default-features = false, features = ["cpal-output"] }

[profile.release]
debug = 1

[features]
scripting = ["rhai"]
default = ["scripting"]

[[example]]
name = "play"
[[example]]
name = "play-script"
required-features = ["scripting"]